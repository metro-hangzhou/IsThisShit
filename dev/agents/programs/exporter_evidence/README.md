# Exporter Evidence Program

This program preserves the exporter-side evidence model, simulator, and reviewer state as a reusable upstream foundation.

Current scope:

- NapCat truth mapping
- evidence dimensions and reachability
- recursive forward families
- result algebra and materialization semantics
- exporter-side retro-review batches

Primary overlays:

- `SHARED_CONTEXT.md`
- `OUTPUT_CONTRACT.md`

Legacy track-local handbooks remain under `dev/agents/subagents/`.
